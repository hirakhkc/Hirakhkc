# Hirak Jyotish Chakra for Windows 10/11 — 3.2.0

The Windows desktop package uses the same report, required Chakra geometry, predictions, time-zone presentation and Demo/Paid activation logic as the Android 3.2.0 edition.

Build with `npm install` followed by `npm run dist` on a Windows or compatible CI runner.
